import React from 'react';

import { Link } from 'react-router';

const Landing = () => {
    return (
        <div>
            <Link to="/creator">New</Link>
        </div>
    );
};

export default Landing;
